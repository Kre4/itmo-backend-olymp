spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb;NON_KEYWORDS=YEAR
    username: postgres
    password: postgres
  flyway:
    enabled: false
  sql:
    init:
      mode: never
